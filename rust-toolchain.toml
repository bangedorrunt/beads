[toolchain]
# Floating nightly, shared with siblings flywheel + toron (operator choice
# 2026-09-06: was pinned nightly-2026-08-20 per ADR-0002 W2 verification,
# unpinned for latest-nightly features). Re-run the gates after each bump —
# new pedantic/nursery lints can fail them.
channel = "nightly"
components = ["rustfmt", "clippy"]
